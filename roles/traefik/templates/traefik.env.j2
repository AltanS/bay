# {{ ansible_managed }}
# Traefik DNS-01 challenge credentials (Cloudflare).
# Consumed via env_file by the traefik container; rendered with mode 0600.
CF_DNS_API_TOKEN={{ traefik_cloudflare_dns_api_token }}

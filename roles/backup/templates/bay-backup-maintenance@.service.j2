# {{ ansible_managed }}
[Unit]
Description=Bay backup maintenance for %i
After=docker.service

[Service]
Type=oneshot
User={{ app_user }}
ExecStart={{ backup_scripts_dir }}/maintenance-%i.sh
Environment=HOME=/home/{{ app_user }}

# Resource limits — be nice to the system
Nice=15
IOSchedulingClass=idle

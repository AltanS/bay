#!/usr/bin/env bash
# {{ ansible_managed }}
# Maintenance script for {{ accessory_name }}
set -euo pipefail

# Restic environment (baked at template time)
export RESTIC_REPOSITORY="{{ repo }}"
export RESTIC_PASSWORD="{{ backup_restic_password }}"
export AWS_ACCESS_KEY_ID="{{ backup_s3_access_key_id }}"
export AWS_SECRET_ACCESS_KEY="{{ backup_s3_secret_access_key }}"
export RESTIC_CACHE_DIR="{{ backup_scripts_dir }}/cache/{{ accessory_name }}"

RESTIC="{{ backup_restic_bin }}"

BAY_ALERT_HEADER='{{ docker_monitor_alert_header }}'
BAY_ALERT_FOOTER='{{ docker_monitor_alert_footer }}'

{% include '_notify.sh.j2' %}

echo "[$(date -Iseconds)] Running integrity check for {{ accessory_name }}..."
if ! "$RESTIC" check --retry-lock 15m 2>&1; then
  bay_notify backup.integrity_check_failed "<b>Integrity check FAILED</b>
Accessory: <code>{{ accessory_name }}</code>
Host: <code>${BAY_HOST}</code>"
  exit 1
fi

"$RESTIC" cache --cleanup 2>&1 || true
echo "[$(date -Iseconds)] Maintenance complete for {{ accessory_name }}"

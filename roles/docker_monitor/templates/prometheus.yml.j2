# {{ ansible_managed }}
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: traefik
    static_configs:
      - targets: ["localhost:{{ traefik_metrics_port }}"]
        labels:
          instance: "{{ inventory_hostname }}"
